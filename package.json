{
  "name": "iina-plugin-youtube-chat",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:plugin": "vite build --config vite.config.plugin.ts --watch",
    "build": "pnpm run build:clean && pnpm run build:typecheck && pnpm run build:sidebar && pnpm run build:plugin && pnpm run build:copy-info",
    "build:clean": "rm -rf dist",
    "build:typecheck": "tsc -b",
    "build:sidebar": "vite build",
    "build:plugin": "vite build --config vite.config.plugin.ts",
    "build:copy-info": "cp plugin/Info.json dist/youtube-chat.iinaplugin/Info.json",
    "check": "biome check --write .",
    "preview": "vite preview",
    "preinstall": "npx only-allow pnpm",
    "prepare": "panda codegen"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "@pandacss/dev": "^1.5.1",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react-swc": "^4.2.2",
    "globals": "^16.5.0",
    "iina-plugin-definition": "^0.99.3",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.46.4",
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
